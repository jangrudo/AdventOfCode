[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "aoc_library"
version = "2024"
dependencies = ["sortedcontainers", "tqdm", "pytest"]
authors = [{name = "Jan Grudo"}]
license = {text = "Public domain"}
