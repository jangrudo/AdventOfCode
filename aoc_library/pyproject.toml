[build-system]
requires = ["setuptools >= 64"]
build-backend = "setuptools.build_meta"

[project]
name = "aoc_library"
version = "2024.12.16"
dependencies = ["numpy", "sortedcontainers", "tqdm", "pytest"]
authors = [{name = "Jan Grudo"}]
license = {text = "Public domain"}
